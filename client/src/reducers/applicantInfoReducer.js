const applicantInfoReducer = (state = [], action) => {
    switch(action.type) {
        case "UPDATE_INFO":
            return state;
        default:
            return state;
    }
}

export default applicantInfoReducer;
